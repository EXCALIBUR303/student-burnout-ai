# Copy this file to .env.local to run against a LOCAL backend instead of Railway.
# The start-all.bat script sets this automatically.
#
# REACT_APP_BACKEND=http://localhost:8000
#
# Leave blank (or delete .env.local) to use the Railway deployment automatically.
